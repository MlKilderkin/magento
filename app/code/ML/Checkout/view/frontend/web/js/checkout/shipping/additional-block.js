define([
    'uiComponent'

], function (Component) {
    'use strict';

    return Component.extend({
        defaults: {
            template: 'ML_Checkout/checkout/shipping/additional-block'
        }
    });
});